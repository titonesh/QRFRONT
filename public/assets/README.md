Place the login background image in this folder with the exact filename `login-bg.jpg`.

Expected path (already referenced by the UI): `/assets/login-bg.jpg` (served from `public/assets/login-bg.jpg`).

Recommended image settings:
- Format: JPEG or WebP
- Filename: `login-bg.jpg` (or `login-bg.webp`; update AdminLogin.jsx if using `.webp`)
- Dimensions: 1920x1080 or wider (keep aspect ratio) — large enough to cover desktop screens
- Quality: 70-85% for good balance of quality and file size
- Optimize: run through an optimizer (e.g., `jpegoptim`, `mozjpeg`, or online compressors) to reduce file size.

If you want me to add the actual image, upload the image file here or provide a URL and I will download and place it at `public/assets/login-bg.jpg` and verify the UI references it.
